# RAAS — Python dependencies (install inside venv: pip install -r requirements.txt)
#
# Optional OS packages for full features (DBus, grim/ffmpeg, input group):
#   See DEPENDENCIES.md (EN) and DEPENDENCIES.tr.md (TR)

requests>=2.28.0
PyYAML>=6.0
evdev>=1.6.0
opencv-python-headless>=4.13.0.92
